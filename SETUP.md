# تشغيل مشروع بسطة محلياً — دليل سريع

هذا الملف إضافي (لا يمس README.md التاريخي) — خلاصة عملية لتشغيل المشروع بعد جلسة إعادة بناء الـ Frontend الكاملة.

> ⚠️ **مهم جداً — هذي النسخة فيها تعديل على `schema.prisma`** (حقل `videoUrl` جديد
> بـ`StorePublicInfo`، إضافة لموديل `CartItem` من نسخة سابقة). **لازم** تشغّلي:
> ```bash
> npm run prisma:generate
> npm run prisma:push
> ```
> **حتى لو كنتِ شغّلتيهم قبل** — وإلا راح تطلع أخطاء type أو "column does not exist".

## 1. المتطلبات
- Node.js 18 أو أحدث
- حساب Supabase (Postgres + Auth + Storage)
- مفاتيح Anthropic و OpenAI (لمنيرة: شات + صوت)
- حساب Upstash Redis (rate limiting)

## 2. التثبيت
```bash
npm install
cp .env.example .env.local
# عبّي .env.local بقيمك الحقيقية (Supabase, Anthropic, OpenAI, Upstash, ENCRYPTION_KEY)
```

## 3. قاعدة البيانات
```bash
npm run prisma:generate
npm run prisma:push       # يُنشئ كل الجداول من schema.prisma مباشرة
npm run prisma:seed       # بيانات تجريبية (اختياري لكن مفيد للتجربة)
```

## 4. التشغيل
```bash
npm run dev
# افتحي http://localhost:3000
```

## 5. ما تغيّر هذه الجلسة (ملخص)
- **Frontend كامل أُعيد بناؤه على theme/** — كل صفحة عامة ولوحتي التاجر والإدارة، صفر لون hardcoded، كل ملف تحت 200 سطر.
- **🆕 سلة تسوق حقيقية + صفحة دفع كاملة:**
  - موديل جديد `CartItem` بـ`schema.prisma` (مربوط بـUser وProduct، نفس نمط WishlistItem الموجود) — **لازم `npm run prisma:push` بعد سحب هذا الكود لإنشاء الجدول**
  - `GET/POST /api/cart`, `PATCH/DELETE /api/cart/[productId]` — إضافية بالكامل
  - `/cart` — سلة مجمّعة حسب المتجر (كل متجر دفعه منفصل لأن كل تاجر عنده بوابة Moyasar خاصة)
  - `/checkout?store=X` — يستخدم `/api/checkout` **الموجود أصلاً بدون أي تعديل** (كان موثّق بتعليقه من البداية إنه يفترض تقسيم السلة حسب المتجر وينادى مرة لكل متجر)
  - زر "أضف للسلة" بصفحة المنتج، وعدّاد حقيقي بأيقونة السلة بالـ Navbar
  - ⚠️ ملخص الطلب بصفحة الدفع يعرض **المجموع الفرعي فقط** — رسوم الشحن الفعلية تُحسب من مزوّد الشحن الحقيقي (متغيّرة، مو 3 ريال ثابتة كما قد يُفهم من الوثيقة — الـ3 ريال هي رسوم بسطة الثابتة المضافة فوق سعر الناقل المتغيّر) وتظهر بالمبلغ النهائي فقط بعد إنشاء الطلب.
- **🆕 لوحة إدارة المدونة بالأدمن:**
  - `GET/POST /api/admin/blog`, `GET/PATCH/DELETE /api/admin/blog/[id]` — إدارة كاملة (مسموحة للأدمن فقط عبر `requireRole`)
  - تاب "📰 المدونة" جديد بلوحة الإدارة — إنشاء/تعديل/حذف مقالات، مع مسودة/نشر وslug تلقائي من العنوان
  - المقالات المنشورة تظهر فوراً بصفحة `/blog` العامة (نفس الـ API الموجود مسبقاً)
  - `GET /api/blog`, `GET /api/blog/[slug]` — تقرأ من موديل `BlogPost` الموجود أصلاً بدون route سابق
  - `GET /api/offers` — تقرأ من موديل `Coupon` الموجود أصلاً بدون route سابق
  - `GET/POST /api/wishlist`, `DELETE /api/wishlist/[productId]` — تقرأ/تكتب فقط على `WishlistItem` الخاص بالمستخدم نفسه
- **🆕 لوحة تحكم تاجر احترافية كاملة (إعادة بناء):**
  - `GET /api/dashboard/summary` — مبيعات/طلبات/منتجات/زوّار حقيقية مع نسبة تغيّر شهرية، + اتجاه مبيعات 7 أيام
  - **تفعيل `StoreAnalytics`** (كان موديل موجود وغير مُستخدم): `POST /api/stores/[slug]/visit` يسجّل كل زيارة حقيقية لصفحة المتجر، مع كوكي لمنع تكرار الزيارة الفريدة لنفس المتصفح خلال 24 ساعة — بدون أي نظام Analytics معقّد
  - رسم بياني SVG مصنوع يدوياً لمبيعات آخر 7 أيام — **بدون أي مكتبة رسوم بيانية جديدة** (يحافظ على سياسة package.json الموثّقة)
  - سايدبار جديد بمجموعات قابلة للطي (المنتجات، المتجر) + نسخة موبايل مسطّحة منفصلة (كانت أول نسخة فيها خلل: العناصر الفرعية تختفي بالكامل على الجوال — انتصلح قبل التسليم)
  - صفحة طلبات جديدة `/dashboard/orders` (كانت مفقودة تماماً رغم إنها كانت مربوطة من القائمة الجانبية)
- **🆕 فيديو المتجر:** حقل `videoUrl` جديد بـ`StorePublicInfo` (إضافي، migration بسيطة) — الآن عندنا 3 أنواع فيديو حقيقية: فيديو المتجر (`StorePublicInfo.videoUrl`)، فيديو المنتج (`Product.videoUrl` — موجود أصلاً)، فيديو قصة الأسرة (`ArtisanStory.videoUrl` — موجود أصلاً)
  - `PATCH /api/stores/[slug]/public-info` — تعديل قصة الأسرة والفيديو من لوحة التاجر (نموذج "إعدادات المتجر السريعة" بالداشبورد)
- **🆕 دفاتري مفعّلة بالكامل (كانت موديلات بدون أي كود يستخدمها):**
  - `GET/POST /api/daftari/entries` — تسجيل قيود يدوية (بيع/مصروف/استرداد/سحب/تعديل) + ملخص الشهر الحالي محسوب حياً
  - محمية بباقة STARTER فما فوق (تطابق حرفياً `SubscriptionPlan` enum)
  - صفحة `/dashboard/daftari` كاملة (ملخص + نموذج إضافة + قائمة قيود) + بطاقة تعريفية بالداشبورد + ذكرها بصفحة الأسعار
- **صفحات جديدة:** `/about`, `/account`, `/favorites`, `/blog`, `/blog/[slug]`, `/offers`, `/cart`, `/checkout`, `/terms`, `/privacy`, `/dashboard/orders`, `/dashboard/daftari`
- **🆕 SEO حقيقي:** `generateMetadata` ديناميكي لكل مقال/منتج/متجر (عنوان + وصف + صورة Open Graph مقروءة من قاعدة البيانات فعلياً) + metadata ثابت لباقي الصفحات العامة + `sitemap.xml` حقيقي (يقرأ كل المقالات المنشورة والمتاجر والمنتجات النشطة) + `robots.txt`. بنية الصفحات "use client" الحالية اتحفظت 100% — فقط انفصلت لمكوّن Client + غلاف Server صغير فوقه (نمط قياسي بـ Next.js App Router)، فلا شي بالمنطق أو الشكل تغيّر.
  - ⚠️ بدّلي `NEXT_PUBLIC_APP_URL` بـ`.env.local` لدومينك الفعلي قبل النشر (يُستخدم بالـ sitemap وrobots.txt)
- **⚠️ صفحات قانونية `/terms` و`/privacy`:** نص عام مبني على حقائق موديل العمل الفعلية، **وليس صياغة قانونية معتمدة** — راجعيها مع محامٍ قبل النشر الرسمي.
- **روابط التواصل الاجتماعي:** صارت قابلة للنقر فعلياً بالفوتر، مركزية بملف `lib/social-links.ts` — **قيمها حالياً placeholder، لازم تستبدليها بحساباتك الحقيقية قبل النشر**.
- **مراجعة شاملة:** صلّحت عدة أخطاء Error-State حقيقية (تابات لوحة الإدارة كانت تفشل بصمت)، وحّدت تسميات حالة الطلب/التذكرة (كانت مكررة بصياغات متضاربة)، وصلّحت سايدبار لوحة التاجر (كان يعصر المحتوى على الجوال).
- **لا تعديل إطلاقاً** على أي route أو DTO أو منطق عمل قديم — فقط إضافات (Additive-only).

## 6. فجوات معروفة (بيانات غير مكتملة عمداً بدل تلفيقها)
| الميزة | الحالة |
|---|---|
| **تخصيص ألوان/مظهر لكل متجر** | مؤجَّلة عمداً بقرارك — لا يوجد حقول لون بـ`Store` model، وما بنيت أي محرر وهمي. تحتاج مرحلة منفصلة (حقول schema جديدة + تطبيق ديناميكي على صفحة المتجر العامة) |
| **`DaftariMonthlySummary`** | الموديل موجود بالـ schema لكن ما زال بدون كود يكتب فيه (تلخيص مُجمَّع مُخزَّن) — استبدلته بحساب حي مباشر من `DaftariEntry` بكل طلب، وهو كافٍ لحجم البيانات الحالي |
| **دفاتري تلقائي من الطلبات** | حالياً تسجيل يدوي فقط (بيع/مصروف تدخله بنفسك) — ربطه التلقائي بحالة "تم التسليم" بالطلبات ميزة لاحقة تحتاج تعديل على منطق تحديث حالة الطلب |
| **دفع السلة متعددة المتاجر** | كل متجر له checkout ودفع منفصل تماماً (لا يوجد Payment Splitting بـMoyasar) — قرار مؤجَّل لحين التأكد من دعم Moyasar لهذه الميزة |
| نقاط بسطة / عضوية ذهبية | لا يوجد أي Points/Loyalty model — غير مبني، ظاهر كـ"قريباً" بصفحة الحساب |
| محفظتي (Wallet) | لا يوجد Wallet model للمشتري — "قريباً" |
| تقييماتي / محادثاتي | لا يوجد endpoint لعرضها — "قريباً" |
| صورة Hero بالصفحة الرئيسية | placeholder لوني — استبدليها بصورة حقيقية (تعليق بالكود يوضّح المكان) |
| عدّادات الصفحة الرئيسية (10,000+ أسرة...) | أرقام تسويقية توضيحية، ليست حيّة |
| أسعار الباقات السنوية | محسوبة تقريبياً (خصم 20%) داخل الكود — تأكدي من `priceYearly` الفعلي |

## 7. لم يُختبر
لم أتمكن من تشغيل `npm run dev` أو `npm run build` فعلياً بهذه البيئة (لا يوجد اتصال إنترنت للسماح بـ `npm install`). راجعت كل الكود يدوياً بعناية (فحص تكرار للألوان، حدود الأسطر، تطابق أشكال استجابة الـ API) لكن يُنصح بتشغيل `npm run build` عندك للتأكد قبل النشر للإنتاج.
